# 전란 그리고 요괴 - 필수 의존성

# 성능 최적화
numpy>=1.21.0          # 벡터화된 계산 (경험치 시스템)
psutil>=5.8.0          # 메모리 모니터링

# 선택적 의존성 (성능 향상)
ujson>=4.0.0           # 빠른 JSON 파싱 (선택사항)
lz4>=3.1.0             # 압축 (세이브 파일, 선택사항)

# 개발 도구 (개발시에만 필요)
memory-profiler>=0.60.0  # 메모리 프로파일링
cProfile                 # 성능 프로파일링 (내장) 